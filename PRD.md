# PRD (Product Requirements Document)
# 칸반보드 애플리케이션

## 1. 제품 개요

### 1.1 목적
사용자가 작업(Task)을 시각적으로 관리할 수 있는 칸반보드 웹 애플리케이션을 개발합니다. 직관적인 드래그 앤 드롭 인터페이스를 통해 작업의 진행 상태를 쉽게 추적할 수 있습니다.

### 1.2 목표 사용자
- 개인 작업 관리가 필요한 사용자
- 프로젝트 진행 상황을 시각적으로 파악하고 싶은 사용자
- 간단한 태스크 관리 도구가 필요한 팀원

### 1.3 핵심 가치 제안
- **간편성**: 복잡한 설정 없이 즉시 사용 가능
- **직관성**: 드래그 앤 드롭으로 작업 상태 변경
- **시각성**: 작업 흐름을 한눈에 파악

## 2. 기능 요구사항

### 2.1 필수 기능 (MVP)

#### F1. 칸반보드 뷰
- **설명**: 세 개의 컬럼으로 구성된 칸반보드 표시
- **컬럼 구성**:
  - To-Do (할 일)
  - In-Progress (진행 중)
  - Done (완료)
- **수락 기준**:
  - 세 개의 컬럼이 가로로 나란히 표시됨
  - 각 컬럼은 제목과 카드 컨테이너로 구성됨
  - 반응형 레이아웃으로 다양한 화면 크기에 대응

#### F2. 카드 생성
- **설명**: 각 컬럼에서 새로운 작업 카드 생성
- **사용자 액션**:
  1. 컬럼 하단의 "Add Card" 버튼 클릭
  2. 텍스트 입력 필드에 작업 내용 입력
  3. Enter 또는 추가 버튼 클릭
- **수락 기준**:
  - 빈 텍스트로는 카드 생성 불가
  - 생성된 카드는 해당 컬럼에 즉시 표시
  - 입력 후 텍스트 필드 초기화

#### F3. 카드 드래그 앤 드롭
- **설명**: 카드를 드래그하여 다른 컬럼으로 이동
- **사용자 액션**:
  1. 카드를 마우스로 클릭하고 드래그
  2. 목표 컬럼으로 이동
  3. 드롭하여 카드 위치 변경
- **수락 기준**:
  - 드래그 시작 시 시각적 피드백 제공 (불투명도 변경)
  - 드래그 중 커서 모양 변경
  - 드롭 가능 영역 하이라이트 표시
  - 드롭 시 카드가 새 컬럼으로 이동
  - 같은 컬럼 내에서도 순서 변경 가능

#### F4. 카드 삭제
- **설명**: 불필요한 카드 제거
- **사용자 액션**:
  1. 카드 우측 상단의 삭제 버튼(×) 클릭
  2. 카드 즉시 제거
- **수락 기준**:
  - 삭제 버튼은 카드 hover 시 명확히 보임
  - 삭제 시 확인 없이 즉시 제거
  - 삭제된 카드는 복구 불가

#### F5. 초기 샘플 데이터
- **설명**: 첫 방문 시 사용법을 보여주는 샘플 카드 표시
- **수락 기준**:
  - To-Do에 2개 샘플 카드
  - In-Progress에 1개 샘플 카드
  - Done에 1개 샘플 카드
  - 샘플 카드도 수정/삭제/이동 가능

### 2.2 Phase 2 기능 (사용자 인증 및 데이터 영속성)

#### F6. 사용자 인증
- **설명**: Supabase를 사용한 다중 인증 방식 지원
- **인증 방식**:
  1. **이메일 인증**: 이메일 + 비밀번호 회원가입 및 로그인
  2. **Google OAuth**: Google 계정으로 소셜 로그인
  3. **GitHub OAuth**: GitHub 계정으로 소셜 로그인
- **수락 기준**:
  - 로그인하지 않은 사용자는 랜딩 페이지로 리다이렉트
  - 로그인 성공 시 사용자별 칸반보드 표시
  - 로그아웃 기능 제공
  - 세션 유지 (브라우저 닫아도 로그인 상태 유지)

#### F7. 데이터 영속성
- **설명**: Supabase PostgreSQL DB에 카드 데이터 저장
- **저장 데이터**:
  - 카드 내용, 소속 컬럼, 순서, 생성/수정 시간
  - 사용자별 데이터 분리 (Row Level Security)
- **수락 기준**:
  - 카드 추가/수정/삭제 시 DB 자동 동기화
  - 새로고침 후에도 데이터 유지
  - 다른 기기에서 로그인 시 동일한 데이터 표시

#### F8. GitHub Pages 배포
- **설명**: 정적 웹사이트 호스팅으로 온라인 접근 가능
- **배포 URL**: `https://<username>.github.io/kosa-vibecoding-2026-2nd/src/exercise/jihye/day03/kanban/`
- **수락 기준**:
  - 온라인에서 누구나 접근 가능
  - HTTPS 지원
  - Supabase와 정상 연동

### 2.3 향후 고려사항 (Phase 3+)
- 카드 내용 수정 기능
- 카드에 설명, 마감일, 태그 추가
- 다크 모드
- 컬럼 추가/삭제/이름 변경
- 다중 보드 관리
- 팀 협업 기능 (보드 공유)

## 3. 비기능 요구사항

### 3.1 성능
- 페이지 로딩 시간: 1초 이내
- 드래그 앤 드롭 반응 속도: 즉각적 (60fps)
- 최대 카드 수: 컬럼당 100개까지 원활한 동작

### 3.2 호환성
- **브라우저**: Chrome, Firefox, Safari, Edge (최신 2개 버전)
- **기기**: 데스크톱, 태블릿 (모바일은 참고용)
- **해상도**: 최소 1024px 너비 권장

### 3.3 사용성
- 처음 사용자가 5분 이내 모든 기능 파악 가능
- 키보드 접근성: 탭 키로 모든 요소 접근 가능
- 명확한 시각적 피드백 제공

### 3.4 유지보수성
- 외부 라이브러리 의존성 없음 (순수 HTML/CSS/JS)
- 명확한 코드 구조와 주석
- 기능 추가 시 기존 코드 수정 최소화

## 4. 제약사항

### 4.1 기술 제약
- 백엔드 서버 없음 (순수 프론트엔드)
- 데이터 영속성 없음 (새로고침 시 초기화)
- 외부 라이브러리 사용 금지

### 4.2 범위 제약
- 단일 사용자 시나리오만 지원
- 실시간 협업 기능 없음
- 모바일 최적화는 필수 아님

## 5. 성공 지표

### 5.1 기능 완성도
- [ ] 모든 필수 기능(F1-F5) 구현 완료
- [ ] 버그 없이 안정적으로 동작
- [ ] 모든 수락 기준 충족

### 5.2 사용자 경험
- [ ] 첫 사용자가 도움말 없이 카드 이동 성공
- [ ] 드래그 앤 드롭이 자연스럽고 직관적
- [ ] 시각적 피드백이 명확함

### 5.3 코드 품질
- [ ] 검증 단계(Verification Steps) 모두 통과
- [ ] 다양한 브라우저에서 정상 동작
- [ ] 코드가 읽기 쉽고 확장 가능

## 6. 릴리스 계획

### Phase 1: MVP 개발 (현재)
- 모든 필수 기능 구현
- 기본 스타일링 완료
- 로컬 환경에서 검증

### Phase 2: 개선 (선택)
- 로컬 스토리지 저장
- 카드 편집 기능
- 애니메이션 개선

### Phase 3: 확장 (선택)
- 다중 보드
- 커스텀 컬럼
- 데이터 내보내기/가져오기
